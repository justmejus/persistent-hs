module Lib
    ( someFunc
    ) where

import Data.Time.Clock

someFunc :: IO ()
someFunc = do
  time <- getCurrentTime
  putStrLn (show time)

myTime = getCurrentTime


mysqr :: Floating a => a -> a
mysqr x = sqrt x + 2
